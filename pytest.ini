[pytest]
# Ensure the repository root is importable so `services.*` packages resolve
# when running the suite from anywhere in the tree.
pythonpath = .

# Revived async services (e.g. the Jira webhook listener) use pytest-asyncio.
# "auto" treats coroutine tests as asyncio tests without per-test markers.
asyncio_mode = auto
