"""interfaces.cli — the `coreline` operator CLI.

A front-end over the deterministic ``core`` package (ADR-0002 §4). It wires the mature
evidence/integrity/storage core into an incident-response workflow and adds the thin
incident-state + audit layer the core does not yet own. No AI, no network.
"""
