"""interfaces.cli — the `coreline` operator CLI.

A front-end over the deterministic ``core`` package (ADR-0002 §4). It wires the mature
incident/evidence/integrity/storage core into an incident-response workflow. No AI, no
network.
"""
