[pytest]
# Run from repo root so `services.slack_orchestrator...` imports resolve.
# `asyncio_mode = auto` lets plain `async def test_*` run without a decorator.
asyncio_mode = auto
testpaths = tests
