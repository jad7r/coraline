# Coreline Jira Webhook Listener Dependencies
#
# Version constraints use lower bounds (>=) so the service installs cleanly on
# current interpreters (Python 3.14) where the original exact pins have no
# wheels. Pin exact versions again if you need reproducible production builds.

# === Web Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# === Data Validation ===
pydantic>=2.9.0
pydantic-settings>=2.5.0

# === Redis (Replay Prevention) ===
# hiredis is optional acceleration; fakeredis is used for offline tests.
redis>=5.0.1

# === Logging ===
# Imported by security/hmac_verifier.py and every module; MUST be installed or
# test collection fails with ModuleNotFoundError: No module named 'structlog'.
structlog>=24.1.0

# === Secrets Management ===
# HMAC webhook secret is loaded from env or the OS keychain (security/secrets.py).
# keyring provides the cross-platform OS keychain backend.
keyring>=25.0.0

# === Testing ===
pytest>=8.3.0
pytest-asyncio>=0.24.0
fakeredis>=2.23.0
httpx>=0.27.0        # ASGI transport for in-process app tests
requests>=2.31.0     # used by tests/mock_jira_webhook_sender.py

# === Development ===
black>=24.0.0
flake8>=7.0.0
mypy>=1.9.0
isort>=5.13.2
